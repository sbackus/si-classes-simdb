%h1 Movies

%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Released
      %th Tagline
      %th Actions
  %tbody.preview-table
    = render @movies

#movie-data-container

= paginate @movies
